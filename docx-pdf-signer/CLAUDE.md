# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

O documento mestre vive um nível acima, em [`../CLAUDE.md`](../CLAUDE.md) — arquitetura,
contratos de API, checklist de robustez, regras de segurança do certificado, comandos e
estado atual de implementação. Mantenha o conteúdo **só lá** para evitar divergência.

Lembrete rápido: o código deste projeto fica nesta pasta (`docx-pdf-signer/`); rode
`npm run dev` / `docker compose up` daqui.
